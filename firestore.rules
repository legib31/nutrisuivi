rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Chaque personne n'accède qu'à ses propres données.
    match /users/{uid}/kv/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
